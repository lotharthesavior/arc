# Arc 0.7.5

- Move the signed-in user's profile and password forms to `/admin/profile`.
- Add Profile and Users links to the generated admin navigation when session auth is installed.
- Protect profile and user administration with narrowly scoped session and idle-timeout middleware.
- Render profile validation and password errors through the styled, escaped profile template.
- Verify the route, login wall, navigation, profile update, and removal of the legacy `/profile` URL
  in a clean generated application and real browser.
