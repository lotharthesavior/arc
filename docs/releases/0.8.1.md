# Arc 0.8.1

- Restore interactive first-administrator setup for generated applications.
- `make setup` now prompts for administrator name, email, and a hidden password when the
  `ARC_SETUP_ADMIN_*` variables are absent.
- Noninteractive setup continues to accept the one-shot environment variables and fails clearly
  when they are missing.
