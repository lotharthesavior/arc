# Arc 0.7.3

- Restrict browser-session middleware to `/admin` instead of an empty scope that intercepted static
  assets.
- Verify `/public/styles.css` remains publicly reachable while `/admin` requires authentication.
- Upgrade existing generated applications when `arc plugin add auth-session` is rerun.
