# Arc 0.5.1

Arc 0.5.1 fixes an unused-variable warning in freshly generated applications before their first
resource API is added.

The generated `api_config` callback now names its parameter `_cfg`, and resource generation uses
that name when registering API modules. The clean-room scaffold gate compiles empty minimal and UI
applications with warnings denied before generating resources, preventing this regression.
