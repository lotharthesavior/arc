# Arc 0.7.1

- Fix generated UI startup panics by registering bundled Tera templates atomically so parent
  layouts resolve regardless of declaration order.
- Add a generator regression test covering bundled template inheritance.
