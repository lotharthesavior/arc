# Arc 0.7.7

- Make repeated auth capability installation idempotent after Cargo reformats multiline plugin
  registrations.
- Remove existing duplicate DB, session, JWT, and RBAC registration calls when a capability is
  reinstalled, repairing applications affected by earlier upgrade instructions.
- Add regression coverage that installs the DB/session/RBAC bundle twice and verifies one
  registration per plugin.
