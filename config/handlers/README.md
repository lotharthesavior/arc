# Handler manifests

Add event-handler manifests here as `*.yaml`. Run `make benthos-config` to
compile them into `config/benthos/generated/events.yaml`.

See `docs/guides/event-handlers.md` for the manifest contract.

