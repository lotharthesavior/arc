[package]
name = "arc-es-postgres"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Postgres EventStore and ReadModelStore implementation for arc event sourcing"
license.workspace = true
repository.workspace = true
readme = "README.md"
keywords = ["event-sourcing", "cqrs", "postgres", "projections", "audit"]
categories = ["database", "asynchronous"]

[dependencies]
# Workspace crates
arc-core = { path = "../arc-core", version = "0.7.2" }

# Database
sqlx.workspace = true

# Core event sourcing dependencies
uuid.workspace = true
serde.workspace = true
serde_json.workspace = true
async-trait.workspace = true
thiserror.workspace = true

# Async runtime
tokio = { workspace = true, features = ["rt", "rt-multi-thread", "macros"] }

# Logging
tracing.workspace = true

[dev-dependencies]
serial_test.workspace = true
arc-core = { path = "../arc-core", version = "0.7.2", features = ["test-utils"] }
