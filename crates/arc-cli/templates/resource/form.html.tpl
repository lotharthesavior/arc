{% extends "layouts/admin.html" %}{% import "components/ui.html" as ui %}{% block content %}<header class="page-head"><div><p class="eyebrow">{{Type}} / {{ mode }}</p><h1>{% if mode == "create" %}New {{Type}}{% else %}Edit {{Type}}{% endif %}</h1></div></header>{% if error %}<div class="alert alert--error" role="alert">{{ error }}</div>{% endif %}<section class="panel"><form method="post"><div style="padding:1rem"><input type="hidden" name="csrf_token" value="{{ csrf_token }}">{% if mode != "create" %}<input type="hidden" name="version" value="{{ form_state.version }}">{% endif %}{% if mode == "create" %}{{ ui::field(name="id", label="Identifier", value=form_state.id) }}{% else %}<input type="hidden" name="id" value="{{ form_state.id }}"><p class="mono">ID / {{ form_state.id }}</p>{% endif %}{{ ui::field(name="name", label="Name", value=form_state.name) }}<button class="button button--primary">Save {{Type}}</button></div></form></section><a href="/admin/{{view}}">Cancel</a>{% endblock content %}
